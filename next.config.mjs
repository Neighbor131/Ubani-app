/**
 * Next.js configuration describing experimental app directory setup and strict mode.
 */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true
  }
};

export default nextConfig;
